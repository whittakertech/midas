# Installation

```bash
gem install whittaker_tech-midas
```

Or in Bundler:

```ruby
gem "whittaker_tech-midas"
```

Run migrations:

```bash
bin/rails railties:install:migrations FROM=whittaker_tech_midas
bin/rails db:migrate
```